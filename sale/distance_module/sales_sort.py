from distance_module import distance_km
from sale.models import Sale, SaleImage
from users_b.models import User

def sales_sort(sales, user_location):
    ''' This function returns a list of sales sorted by distance '''
    pass